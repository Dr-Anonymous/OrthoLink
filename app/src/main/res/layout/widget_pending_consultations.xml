<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/widget_background"
        android:orientation="vertical">

        <!-- Title Bar Container with background color -->
        <LinearLayout
            android:id="@+id/widget_title_bar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/widget_title_bg"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:paddingHorizontal="12dp"
            android:paddingVertical="8dp">

            <TextView
                android:id="@+id/widget_title"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Consultations"
                android:textColor="@color/colorOnPrimary"
                android:textSize="16sp"
                android:textStyle="bold" />

            <ImageView
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:src="@drawable/ic_refresh" 
                android:contentDescription="Refresh"
                app:tint="@color/colorOnPrimary"
                android:visibility="visible"
                xmlns:app="http://schemas.android.com/apk/res-auto"/> 
             <!-- Removed widget_loader from here since we use overlay -->

        </LinearLayout>

        <ListView
            android:id="@+id/widget_list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scrollbarStyle="outsideOverlay"
            android:padding="4dp"
            android:divider="@android:color/transparent"
            android:dividerHeight="4dp"
            android:clipToPadding="false" />

        <TextView
            android:id="@+id/widget_empty_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:text="No consultations for today."
            android:textColor="@color/colorTextSecondary"
            android:visibility="gone" />

    </LinearLayout>

    <RelativeLayout
        android:id="@+id/widget_loading_overlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#80000000"
        android:clickable="true"
        android:visibility="gone">

        <ProgressBar
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:indeterminateTint="@color/colorOnPrimary" />

    </RelativeLayout>

</FrameLayout>
